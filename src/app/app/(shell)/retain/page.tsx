import StubPage from "../shell/StubPage";

export const dynamic = "force-dynamic";

export default function Page() {
  return (
    <StubPage
      title="Retain"
      intro="Confirmations, reminders and no-show recovery on every booking."
      line="Remind & Reschedule isn't running for your account yet. Once it is, this page shows the reminder chain, replies, and no-shows recovered."
    />
  );
}
