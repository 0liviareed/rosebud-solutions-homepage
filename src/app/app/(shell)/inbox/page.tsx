import StubPage from "../shell/StubPage";

export const dynamic = "force-dynamic";

export default function Page() {
  return (
    <StubPage
      title="Handed to your team"
      intro="Enquiries that need a person, in one place."
      line="Arrives with Qualify & Triage. When an enquiry needs a human — an urgent symptom, a question outside your rules, a record to confirm — it will land here with what to do next."
    />
  );
}
