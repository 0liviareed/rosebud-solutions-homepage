import StubPage from "../shell/StubPage";

export const dynamic = "force-dynamic";

export default function Page() {
  return (
    <StubPage
      title="Reactivate"
      intro="Dormant and not-yet-ready enquiries brought back on a sequence."
      line="Recall & Nurture isn't running for your account yet. Once it is, this page shows sequences in flight, replies and re-entries to qualifying."
    />
  );
}
